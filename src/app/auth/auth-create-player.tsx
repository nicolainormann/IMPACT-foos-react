import { Component, h } from "preact";

export class AuthCreatePlayer extends Component {
    render() {
        const authCreatePlayer = (
            <div class="auth-create-player">
                Create player
            </div>
        );

        return authCreatePlayer;
    }
}
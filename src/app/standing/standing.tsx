import { Component, h } from "preact";

export class Standing extends Component {
    render() {
        const standing = (
            <div class="standing">
                Standing
            </div>
        );

        return standing;
    }
}